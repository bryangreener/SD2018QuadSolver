Folder contents:

- makefile
  Temp makefile. Not in use.
 
- rline.c
  rline method that prompts user for input, reads input, then sends back to
  main method as char array.
 
- rline.h
  Header file for rline method that includes std libraries for reading input.

- rline.o
  Object file generated by makefile in root directory.

- src
  Directory following src directory structure in root.

Copyright © 2018, Bryan Greener (https://github.com/bryangreener).
